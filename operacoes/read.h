#ifndef READ_H
#define READ_H

/*
 * Funcionalidade 2 – SELECT * FROM:
 * Lê o arquivo binário 'arqBin', percorre todos os registros e imprime
 * na saída padrão os que não estão logicamente removidos.
 * Imprime "Registro inexistente." se não houver registros ativos.
 */
void func2(char *arqBin);

#endif
